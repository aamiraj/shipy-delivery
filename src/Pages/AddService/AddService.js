import React from "react";
import useTitle from "../../hooks/useTitle";

const AddService = () => {
  useTitle("Add service");
  return (
    <div>
      <h1>This is add services page/....</h1>
    </div>
  );
};

export default AddService;
